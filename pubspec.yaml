name: FitStack
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: '>=2.16.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  #http
  retrofit: ^3.0.1+1

  #util
  device_info_plus: ^4.1.2

  #annotation
  json_annotation: ^4.6.0
  copy_with_extension_gen: ^4.0.3

  # state
  flutter_bloc: ^8.0.1
  hydrated_bloc: ^8.1.0
  shared_preferences:
  equatable:
  meta: ^1.3.0
  very_good_analysis: ^3.1.0
  flutter_form_builder: ^7.4.0
  form_builder_validators: ^8.2.0

  #cache/storage
  # use hive
  get_storage: ^2.0.3
  flutter_secure_storage: ^6.0.0

  #ux
  clipboard: ^0.1.3
  pull_to_refresh: ^2.0.0

  # ui
  persistent_bottom_nav_bar: ^5.0.2
  lottie: ^1.3.0
  rive: ^0.9.1
  carousel_slider: ^4.1.1
  syncfusion_flutter_charts: ^20.2.39
  syncfusion_flutter_datepicker: ^20.2.39
  dropdown_button2: ^1.6.3
  auto_size_text: ^3.0.0
  badges: ^2.0.3
  flutter_slidable: ^2.0.0
  modal_bottom_sheet: ^2.1.2
  anim_search_bar: ^2.0.2
  fluttertoast: ^8.1.1
  animations: ^2.0.7

  #theme
  flex_color_scheme: ^6.0.1

  #gameify
  achievement_view: ^1.0.0

  #auth
  email_validator: ^2.0.1

  # fonts
  google_fonts: ^3.0.1
  font_awesome_flutter: ^10.1.0
  line_icons: ^2.0.1

  # Images
  flutter_svg: ^1.0.3
  image_picker: ^0.8.6

  # for debug
  device_preview: ^1.0.0

  # health
  health: ^4.1.1

  # routing
  go_router: ^5.1.1

  #firebase
  firebase_core: ^1.23.0
  firebase_crashlytics: ^2.8.11
  firebase_analytics: ^9.3.6
  firebase_performance: ^0.8.3+1
  firebase_storage: ^10.3.9
  firebase_dynamic_links: ^4.3.9
  firebase_auth: ^3.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  flutter_lorem: ^2.0.0
  flutter_launcher_icons: ^0.10.0
  build_runner: ^2.2.0
  json_serializable: ^6.3.1

flutter_icons:
  android: true
  ios: true
  image_path: 'assets/app/icons/FitStackIosLauncherIcon.png'

flutter:
  uses-material-design: true
  assets:
    - assets/app/icons/
    - assets/app/images/
